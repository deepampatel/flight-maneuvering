services:
  intercept:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - PYTHONPATH=/app:/app/backend
    volumes:
      - recordings:/app/backend/recordings
    restart: unless-stopped

volumes:
  recordings:
